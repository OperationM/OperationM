$('#commentListItem<%= @comment.id %>').remove()
<% if @movie.comments.size > 10 %>
  $('.commentViewAll a').text('View all <%= @movie.comments.size %> comments')
<% else %>
  $('.commentViewAll a').remove()
<% end %>
