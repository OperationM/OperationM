$('#commentList').prepend("<%= escape_javascript(render :partial => 'comments/comment_item', :locals => {:comment_item => @comment}) %>");
$("#new_comment")[0].reset();
$("#commentList").scrollTop(0);